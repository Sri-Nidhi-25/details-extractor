# For PDF text extraction
PyPDF2>=3.0.0

# For image OCR (pytesseract requires Tesseract binary installed separately)
pytesseract>=0.3.10
Pillow>=10.0.0

# Optional but recommended fallback for handwriting
paddlepaddle
paddleocr
opencv-python

# Everything else is not required for your pipeline.